# Що НЕ вантажити в хмару збірки.
#
# Шаблони прив'язані до кореня (/) НАВМИСНО. Без слеша `android/` збігається з
# будь-якою текою з такою назвою — зокрема з modules/track-guard/android, де
# лежить нативний код власного модуля. Тобто збірка мовчки поїхала б без нього.
/node_modules
/.expo
/dist
/web-build
/android
/ios
*.apk
*.aab
