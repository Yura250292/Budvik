import { redirect } from "next/navigation";

/**
 * Кабінет показників переїхав на головну секції: /sales відкривається
 * одразу на планах і показниках, окремого хабу більше немає.
 *
 * Редирект, а не видалення: адреса розійшлась по закладках торгових і
 * по старих посиланнях, і 404 на ній читалась би як «кабінет зламався».
 */
export default function SalesAnalyticsRedirect() {
  redirect("/sales");
}
