{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["fra1"],
  "crons": [
    {
      "path": "/api/cron/webstats-rollup",
      "schedule": "30 1 * * *"
    }
  ],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ':(exclude)scripts' ':(exclude)output' ':(exclude)design' ':(exclude)worker' ':(exclude)agent' ':(exclude)*.md'"
}
