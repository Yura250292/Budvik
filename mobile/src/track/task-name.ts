/**
 * Ім'я фонового завдання — окремим модулем навмисно.
 *
 * Його потребують і той файл, що завдання оголошує (task.ts), і той, що його
 * запускає (controller.ts). Якби ім'я жило в одному з них, другий тягнув би за
 * собою весь модуль — і оголошення завдання виконувалося б у момент, коли
 * застосунок цього ще не чекає.
 */
export const TRACK_TASK = "budvik-track-location";
/** Періодична перевірка: чи не заснула служба, чи не завис буфер. */
export const WATCHDOG_TASK = "budvik-track-watchdog";
/** Геозона навколо місця, де закрилася зміна: будить нас, коли машина поїхала. */
export const AFTER_SHIFT_TASK = "budvik-after-shift-geofence";
/**
 * Пробудження сповіщенням — остання, і найсильніша, страховка треку.
 *
 * Сервер бачить, що зміна відкрита, а точок немає, і шле планшету
 * сповіщення. Саме ДОСТАВКА, а не дотик людини, будить це завдання — і в
 * ту мить застосунок опиняється в тимчасовому білому списку системи, де
 * Android дозволяє підняти службу переднього плану з фону. Інакше це
 * заборонено з 12-ї версії, і саме тому вбитий запис місяць не оживав сам.
 */
export const WAKE_TASK = "budvik-track-wake";
