import MeetingsScreen from "./MeetingsScreen";

export const metadata = { title: "Наради" };
export const dynamic = "force-dynamic";

export default function MeetingsPage() {
  return <MeetingsScreen />;
}
