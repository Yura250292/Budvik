-- Вимкнення категорій пушів стрічки торгового — у профілі, по людині.
--
-- Один JSON замість колонки на категорію: категорій буде більше (картка
-- перед візитом, список дзвінків), і кожна нова не має коштувати міграції.
-- NULL означає «усе ввімкнено»; вимкнена категорія лишається в стрічці на
-- головній, зникає лише пуш. Це запобіжник проти єдиної реальної загрози
-- каналу — що людина вимкне сповіщення застосунку цілком, разом із
-- сигналом «підніми трек».
ALTER TABLE "User" ADD COLUMN "notificationPrefs" JSONB;
