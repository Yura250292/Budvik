# Що НЕ вантажити в хмару збірки.
#
# Архів проєкту виходив 256 МБ, і майже все це — не потрібне для збірки:
# EAS все одно ставить залежності сам. Довге вивантаження на слабкому каналі
# — найчастіша причина «збірка не стартувала».
node_modules/
.expo/
dist/
web-build/
android/
ios/
*.apk
*.aab
