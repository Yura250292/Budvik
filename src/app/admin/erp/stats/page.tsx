import { redirect } from "next/navigation";

/**
 * «Статистика» переїхала в «Аналітику». Вкладки «Закупівлі» там більше
 * немає — прихідні з 1С ще не синхронізуються, тож вона показувала нулі;
 * повернеться разом із даними. Поки ведемо на огляд.
 *
 * Редірект, а не видалення: посилання на /admin/erp/stats розійшлися по
 * закладках.
 */
export default function StatsRedirect() {
  redirect("/admin/analytics");
}
