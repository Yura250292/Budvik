/** Ринкова ціна, старша за це, для пропозиції вже не годиться. */
export const MARKET_FRESH_DAYS = 14;
/** Товар, для якого агент нічого не знайшов, шукаємо знову не раніше ніж через стільки днів. */
export const LOOKUP_AGAIN_DAYS = 60;
