/**
 * Ім'я фонового завдання — окремим модулем навмисно.
 *
 * Його потребують і той файл, що завдання оголошує (task.ts), і той, що його
 * запускає (controller.ts). Якби ім'я жило в одному з них, другий тягнув би за
 * собою весь модуль — і оголошення завдання виконувалося б у момент, коли
 * застосунок цього ще не чекає.
 */
export const TRACK_TASK = "budvik-track-location";
/** Періодична перевірка: чи не заснула служба, чи не завис буфер. */
export const WATCHDOG_TASK = "budvik-track-watchdog";
