/**
 * Логін і пароль торгового — історична адреса.
 *
 * Логіка переїхала в /api/admin/users/[id]/credentials, коли керування
 * доступами зібрали в один розділ: цей ендпоінт умів лише SALES, тож
 * складовщику чи водієві задати пароль було неможливо.
 *
 * Файл лишається реекспортом, а не видаляється: на нього ходить вкладка
 * «Доступ» у /admin/sales-reps/[id], і в браузерів може лежати стара
 * сторінка. Реекспорт знімає вимогу деплоїти фронт і бекенд одночасно.
 *
 * Нова перевірка прав ширша за стару (PROTECTED_ROLES замість
 * `role !== "SALES"`), але не слабша: креденшели ADMIN/MANAGER, як і
 * раніше, недоступні менеджеру.
 */
export { GET, PATCH } from "@/app/api/admin/users/[id]/credentials/route";
