/**
 * Нижня межа аналітики — дата, з якої в базі є ОБИДВА потоки документів.
 *
 * Повернення бекфілились із 2023-01-01, а реалізації — лише з 2026-01. Через
 * це період 2023–2025 складається з самих мінусів: 2259 повернень на 4,34 млн
 * грн, під якими немає жодного продажу. Звіт за ті місяці показував би
 * від'ємний оборот — не «мало продали», а буквально мінус, якого не було.
 *
 * Живе в окремому файлі, бо потрібна двом шарам одразу: period.ts розбирає
 * запит користувача (і не має тягнути за собою Prisma), facts.ts підставляє
 * межу в SQL. Спільний імпорт гарантує, що обидва бачать те саме число.
 *
 * Прибрати межу можна буде разом із бекфілом реалізацій на ту саму глибину
 * (механіка є — прапорець realizationsBackfilledAt у agent/ps/state.json).
 */

/** YYYY-MM-DD за Києвом — для порівняння з fromDay/toDay у period.ts. */
export const ANALYTICS_SINCE_DAY = "2026-01-01";
