/**
 * Токени робочого кабінету — з макета ~/Desktop/pencil-sales.pen.
 *
 * Окремо від src/theme.ts навмисно. Той описує вітрину для покупця: білий фон,
 * жовті кнопки, чорна шапка. Робочі екрани живуть за іншим правилом — фірмовий
 * жовтий лишається ЛИШЕ за керуванням (кнопка дії, активна вкладка), а дані
 * фарбуються станом: зелене — прийнято, жовтогаряче — чекає, червоне — не
 * вийшло. Змішати ці дві мови означало б, що жовтий на екрані водія читається
 * то як «натисни», то як «увага».
 *
 * Числа й кольори взяті з макета один в один, щоб верстку можна було звірити
 * з ним, а не сперечатися про відтінок.
 */

export const c = {
  /** Фірмовий жовтий. Тільки керування: головна кнопка, активна вкладка. */
  brand: "#FFD600",
  /** Чорний шапки й таб-бару. */
  bk: "#0A0A0A",
  bkSoft: "#1C1C1C",

  /** Полотно екрана — тепло-сіре, щоб білі картки на ньому читалися як картки. */
  bg: "#F4F4F2",
  surface: "#FFFFFF",
  line: "#E8E8E6",

  text: "#0A0A0A",
  /** Підпис у парі «назва — значення». */
  text2: "#5B6068",
  /** Третій план: одиниці, дати, пояснення дрібним. */
  text3: "#8A8F98",

  good: "#059669",
  goodFg: "#047857",
  goodBg: "#ECFDF5",
  goodLine: "#A7F3D0",

  warn: "#D97706",
  warnFg: "#B45309",
  warnBg: "#FFFBEB",
  warnLine: "#FDE68A",

  bad: "#DC2626",
  badFg: "#B91C1C",
  badBg: "#FEF2F2",
  badLine: "#FECACA",

  info: "#2563EB",
  infoFg: "#1D4ED8",
  infoBg: "#EFF6FF",

  /** Рамка поля вводу й другорядної кнопки. */
  inputLine: "#D1D5DB",

  onDark: "#FFFFFF",
  /** Підпис на темному: сірий, а не напівпрозорий білий — на градієнті рівніший. */
  onDarkMuted: "#9CA3AF",
  onDarkFaint: "#FFFFFF73",
  onDarkFill: "#FFFFFF14",
} as const;

export const r = {
  card: 16,
  btn: 12,
  /** Малий радіус: вкладені кнопки всередині картки, цифри одометра. */
  sm: 10,
  xs: 6,
  chip: 999,
} as const;

export const sp = {
  /** Поля екрана й картки. */
  pad: 16,
  /** Відстань між картками і між рядками всередині них. */
  gap: 12,
  sm: 8,
  xs: 6,
  xxs: 3,
} as const;

/**
 * Висота головної кнопки — 52. Не «щоб було красиво»: у неї цілять пальцем,
 * тримаючи кермо або коробку, і 44 (мінімум за гайдлайном) тут замало.
 */
export const H = { primary: 52, secondary: 44, field: 52, link: 44 } as const;

export const type = {
  /** Заголовок екрана в шапці. */
  title: { fontSize: 20, fontWeight: "700" },
  /** Надзаголовок над ним — стан, дата, крок. Завжди великими. */
  eyebrow: { fontSize: 11, fontWeight: "500", letterSpacing: 0.4 },
  /** Заголовок картки-стану. */
  cardBig: { fontSize: 17, fontWeight: "700" },
  /** Заголовок звичайної картки. */
  card: { fontSize: 15, fontWeight: "700" },
  /** Підпис групи всередині картки. */
  label: { fontSize: 14, fontWeight: "400" },
  value: { fontSize: 14, fontWeight: "600" },
  body: { fontSize: 13, fontWeight: "400", lineHeight: 19 },
  small: { fontSize: 12, fontWeight: "400", lineHeight: 17 },
  tiny: { fontSize: 11, fontWeight: "400" },
} as const;
