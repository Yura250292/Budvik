-- Кілометри, що прийшли прямою через провал у даних.
--
-- Провал розпізнається за часом: 16 596 метрів за 81 секунду — це не поїздка,
-- яку ми виміряли, а дві точки з дірою між ними. У пробігу вони лишаються
-- (людина відстань проїхала), але окремим числом.
ALTER TABLE "Shift" ADD COLUMN "gapKm" DOUBLE PRECISION;
