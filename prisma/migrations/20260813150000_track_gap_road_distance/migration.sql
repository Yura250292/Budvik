-- Розриви треку: довжина відрізка дорогою + її геометрія.
--
-- Коли планшет був офлайн, дві сусідні точки з'єднувались прямою, і в
-- пробіг ішла хорда — коротша за реальну дорогу. Ці поля заповнюються
-- лише для таких відрізків; там, де трек щільний, вони лишаються NULL і
-- пробіг рахується як раніше.
ALTER TABLE "TrackPoint" ADD COLUMN "roadMetersFromPrev" INTEGER;
ALTER TABLE "TrackPoint" ADD COLUMN "gapGeometry" JSONB;
