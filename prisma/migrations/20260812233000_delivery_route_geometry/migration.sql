-- Геометрія обраного маршруту (GeoJSON LineString з OSRM).
--
-- Досі лінію маршруту було видно лише в момент вибору варіанта: планшет
-- тримав її в стані сторінки, і після перезавантаження водій знову бачив
-- голі точки. Зберігаємо при застосуванні порядку — і день відкривається
-- одразу з лінією, без повторного запиту до OSRM.

ALTER TABLE "DeliveryRoute" ADD COLUMN "routeGeometry" JSONB;
