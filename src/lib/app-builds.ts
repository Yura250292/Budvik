/**
 * Які збірки застосунків лежать у сховищі.
 *
 * Числа тут, а не з двійкового AndroidManifest усередині APK: розбір того
 * формату — окрема залежність, яка ламається від зміни формату заради двох
 * чисел. А так версія видна в дифі поруч із ключем, і розбіжність помітна
 * одразу.
 *
 * Ключ версіонований навмисно: попередня збірка лишається доступною, тож
 * відкотитися можна зміною одного рядка, а не перезаливанням файла.
 */

/** Застосунок покупця: ua.budvik.shop, роздається з /admin/mobile-app. */
export const SHOP_APK_KEY = "app/Budvik27-1.0.0.apk";
export const SHOP_APK_VERSION_CODE = 1;
export const SHOP_APK_VERSION_NAME = "1.0.0";

/**
 * Робоча збірка: ua.budvik.staff, роздається зі сторінок /sales/app і /driver/app.
 *
 * Замінює нативний BudvikTracker (Kotlin). Поки обидва в обігу, номери версій у
 * них незалежні: у трекера свій лічильник (6 / «1.5»), у цієї збірки — власний,
 * і плутати їх не можна, бо застосунки питають різні адреси.
 *
 * versionCode рахується з версії за формулою major*10000 + minor*100 + patch і
 * мусить збігатися з тим, що поклала збірка EAS (див. mobile/app.config.ts).
 * Розбіжність означає, що людина побачить кнопку «Оновити», яка ставить ту саму
 * версію — або, гірше, не побачить її взагалі.
 *
 * ПОРЯДОК ВАЖЛИВИЙ: ці числа описують те, що ЛЕЖИТЬ У СХОВИЩІ, а не те, що
 * зібрано. Піднімати їх можна лише ПІСЛЯ заливання файла. Інакше роут версії
 * не знаходить ключа, віддає 503 — і картка встановлення зникає з обох
 * сторінок разом із банером у кабінеті, тобто ставити стає нічого. Один раз
 * я вже підняв їх наперед і мало не викотив саме це.
 */
export const STAFF_APK_KEY = "app/BudvikStaff-1.6.9.apk";
export const STAFF_APK_VERSION_CODE = 10609;
export const STAFF_APK_VERSION_NAME = "1.6.9";

/**
 * З чого зібрано те, що СПРАВДІ стоїть у полі.
 *
 * Двома числами версії цього не видно, і 01.09 це коштувало дня маршруту в
 * трьох торгових. APK 1.3.0 залили о 13:26, а виправлення, через яке буфер
 * треку висів на мертвому запиті, лягло того ж дня о 16:57 — на три з
 * половиною години пізніше. Версія в сховищі й версія на планшетах збігалися,
 * тож усе виглядало правильно: «нова збірка стоїть, а трек не йде». Насправді
 * стояла та сама 1.3.0, тільки без виправлення.
 *
 * APK — це нативна оболонка, а JS доїжджає окремо через `eas update`, тож
 * коміти два. Розбіжність із HEAD показує `npm run check:staff` — і саме він
 * відповідає на питання «у полі вже з виправленням чи ще ні».
 *
 * Оновлювати: STAFF_APK_COMMIT — коли залито новий APK, STAFF_OTA_COMMIT —
 * після кожного `npm run update:staff`.
 */
/**
 * 1.6.9 (f603a7e, EAS e8a02721) — доставка координат на Android 16. Кожна нова
 * порція скасовувала роботу доставки й ставила її наново зі свіжим терміном у
 * хвилину, а координати приходять раз на десять секунд — термін не настав би
 * ніколи. На планшеті Олександра (Lenovo TB336ZU, Android 16) з 598 поставлених
 * робіт виконалося 24, тоді як на Android 14/15 виконуються всі. Тепер термін
 * рахується від першого планування.
 *
 * Містить і 1.6.8: живий контекст під підпискою JS більше не стирається
 * (після 1.6.7 перехоплення черги змінило порядок подій, і трюк «не закривати
 * першу подію» захищав уже не ту — 116 подій у черзі без читача).
 *
 * Перевірено в dex: budvikRemainingDeadline, budvikJobStarted, jobOverdue,
 * «скасовано: контекст слухає JS», budvikFindObserved, «перехоплення»,
 * budvikPick, «Android відмовив у запуску служби»; підпис AD:FD:DE:CD…C0:8B.
 * 1.6.8 (EAS b8244a5f) зібрано й перевірено, але не роздавалося: 1.6.9 містить усе з неї.
 *
 * 1.6.7 (e3e5afc, EAS bfe2c04d) — трек більше не тоне в модулі, якого JS не слухає.
 * У процесі буває кілька екземплярів TaskManagerInternalModule; диспетчер тримав
 * останній, а JS підписувався на свій, і події лягали в чергу без читача (планшет
 * Передрія: 8619 відданих, 0 оброблених, п'ять днів без треку). Тепер підписка JS
 * робить свій екземпляр активним і забирає застряглі черги, непідписаний не
 * витісняє підписаного, а в знімку видно, хто активний і скільки копій живе.
 * Перевірено в dex: «перехоплення», «відхилено id=», budvikDrainOthers, budvikPick,
 * activeObserved, «внутрішнього модуля немає»; старі рядки 1.6.5/1.6.6 на місці;
 * підпис AD:FD:DE:CD…C0:8B.
 *
 * 1.6.6 (6605092, EAS a39131dc) — чорна скринька треку: нативний маяк track-guard
 * (/api/track/native-beacon з будильника, після boot і оновлення — без JS),
 * журнал диспетчера expo-task-manager (BudvikTaskDiag: хто знімав завдання, черга,
 * стирання реєстрації контексту), лічильники доставки в LocationTaskConsumer,
 * причини смерті процесу. Для buildFromSource тепер три модулі: expo-location,
 * expo-task-manager і unimodules-app-loader (без останнього Gradle падає, збірка
 * 10fcc034). Перевірено 16.09: у dex BudvikTaskDiag/budvikState/budvikDiag і
 * «Android відмовив у запуску служби», підпис AD:FD:DE:CD…C0:8B як у 1.6.5.
 *
 * 1.6.5 (1618810) — перша збірка, в якій патч expo-location справді є в коді:
 * до неї EAS брав готовий AAR і правка сирців не потрапляла в APK (1.6.4 з
 * 19d1788 патча НЕ містить). Перевіряти так: `unzip classes*.dex` і
 * `grep -a -c 'Android відмовив у запуску служби'` — має бути ≥1.
 */
export const STAFF_APK_COMMIT = "f603a7e";
/**
 * 22.09.2026: 8804b56 на всіх чотирьох живих оболонках — застосунок сам перезапускає
 * контекст JS, коли диспетчер віддає події, а до нього не доходить жодна
 * (mobile/src/track/deaf-dispatch.ts; причина — кілька екземплярів
 * TaskManagerInternalModule, див. коміт f87f17a). Групи: 1.6.7 0090f8c4,
 * 1.6.6 372479b7, 1.6.5 a7611b65, 1.5.1 050d88c3. Бандл у всіх чотирьох один і той
 * самий (entry-f257d47e…hbc), хоча в перших трьох записано коміт 3517962: між
 * публікаціями в дереві з'явився новий коміт, JS від цього не змінився.
 *
 * Передрія це НЕ рятує: оновлення завантажує той самий код, який у нього глухий.
 * Йому потрібна примусова зупинка застосунку або APK 1.6.7.
 *
 * 15.09.2026 ввечері поле одне: 4567623 на всіх живих оболонках — повна
 * діагностика в пульсі (mobile/src/track/diag.ts: канали пробудження контексту,
 * утримане перше завдання, нативний знімок, якщо оболонка вміє), подія `restart`
 * і людський текст про застряглий встановлювач APK. Нативний маяк і журнал
 * диспетчера — лише з APK 1.6.6. Групи: 1.6.5 718c6d83, 1.6.4 8d98ceb4,
 * 1.6.1 dbf08558, 1.5.1 5e50b37e (1.6.2 у полі вже немає). Перша спроба зависла
 * на публікації в Expo і нічого не опублікувала. Без чужої правки cabinet.tsx.
 *
 * Відкат на 6330fdc (mobile/, `npx eas update:republish --group <id>`):
 * 1.6.5 0f5ded40, 1.6.4 83450a5d, 1.6.1 4cb0df79, 1.5.1 d33fad6b.
 *
 * 15.09.2026 поле одне: 6330fdc на всіх живих оболонках — патч expo-task-manager
 * (mobile/patches/expo-task-manager+14.0.9.patch): перше фонове завдання кожного
 * контексту JS не закривається, тож TaskService не стирає реєстрацію живого
 * контексту («служба жива, пачок 0» у Джумаги, Кулика й Валентина 15.09).
 * Групи: 1.6.5 0f5ded40, 1.6.4 83450a5d, 1.6.2 44640dc9, 1.6.1 4cb0df79,
 * 1.5.1 d33fad6b. Опубліковано без незакоміченої чужої правки cabinet.tsx.
 *
 * Відкат на 66ba4b1 (mobile/, `npx eas update:republish --group <id>`):
 * 1.6.5 e1579582, 1.6.4 c7fcde22, 1.6.2 244b0ac9, 1.6.1 95c67a97, 1.5.1 2bfd09a3.
 *
 * 14.09.2026 поле одне: 66ba4b1 на всіх живих оболонках — записувач більше не
 * ріже поїздки на Lenovo (mobile/src/track/fix-gate.ts), лічильники фіксів у
 * пульсі. Групи: 1.6.4 c7fcde22, 1.6.2 244b0ac9, 1.6.1 95c67a97, 1.5.1 2bfd09a3
 * (і наперед під 1.6.5 e1579582).
 *
 * Відкат на ac08ab4 (mobile/, `npx eas update:republish --group <id>`):
 * 1.6.4 b6fef5e5, 1.6.2 7338b436, 1.6.1 7a741be2, 1.5.1 1d084bca.
 *
 * 13.09.2026 поле було одне: ac08ab4 на всіх живих оболонках (1.6.4, 1.6.2,
 * 1.6.1, 1.5.1) — заставка запуску, виправлення 12.09 і сторож першого
 * завантаження кабінету.
 *
 * «Білий екран» 12.09, через який поле відкочували на ef21543, не був падінням
 * JS. 13.09 його спіймали логами: WebView після 302 з /api/device/session не
 * запитував сторінку кабінету 85 секунд, без жодної помилки. Тепер застосунок
 * сам перестворює WebView, а кожне застрягання пише `cabinet_stall` у журнал
 * пристрою — причину ще треба знайти.
 *
 * Відкат (mobile/, `npx eas update:republish --group <id>`):
 *  • заставка без сторожа (af02dad): 1.6.4 6edc58eb, 1.6.2 255e2f8e,
 *    1.6.1 b973af53, 1.5.1 7b7cc1f0;
 *  • до заставки (ef21543/19d1788): 1.6.4 a4fd175e, 1.6.2 d54eee5d,
 *    1.6.1 0b5c6643, 1.5.1 d6074ace.
 */
export const STAFF_OTA_COMMIT = "8804b56";

/**
 * Нижче цього номера застосунок не пускає до роботи, а вимагає оновитися.
 *
 * Потрібно на випадок, коли стара збірка починає шкодити даним — наприклад,
 * шле трек у форматі, який сервер уже не приймає.
 *
 * Лишається 1.1.0, хоча поточна 1.3.0: ні в 1.1, ні в 1.2 немає нічого, що
 * псує дані, — вони просто гірше відновлюють трек без мережі й не вміють
 * показати системний діалог батареї одним дотиком. Блокувати ними роботу
 * означало б зупинити людину серед дня заради поліпшення, а не виправлення.
 */
export const STAFF_MIN_VERSION_CODE = 10100;
