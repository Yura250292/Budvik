-- Повернення товару від покупця (Документ.ВозвратТоваровОтПокупателя).
--
-- Проба probe-returns.ps1 на цій базі: 2099 документів за три роки на
-- 4,6 млн грн. Досі вони не читалися взагалі, тому оборот і КПІ торгових
-- завищені рівно на цю суму — при тому що дебіторка їх уже враховує
-- (1С сама зменшує борг), тобто дві цифри в системі суперечать одна одній.
--
-- Повернення лягають у ту саму таблицю SalesDocument третім типом. Усі
-- наявні споживачі фільтрують docType явно, тож нові рядки для них невидимі,
-- доки фільтри аналітики не розширять свідомо.
ALTER TYPE "SalesDocType" ADD VALUE 'RETURN';
