/**
 * Чи показувати ТОРГОВОМУ пробіг за GPS і порівняння з одометром.
 *
 * Вимкнено з 12.09.2026, і це не косметика.
 *
 * ЩО СТАЛОСЯ. Поки трек ламається (застряглий контекст, служба, яку Android
 * не піднімає з фону), число «за GPS» описує не поїздку, а те, скільки точок
 * встигло доїхати до сервера. 11.09 Кулик закрив зміну, і застосунок показав
 * йому «за GPS 46,3 км проти 228 за одометром, співвідношення 4,92 —
 * перевірте число», а зверху ще й вікно «Перевірте одометр: за маршрутом
 * виходить 46.3 км». Людина ввела правильне число з приладу, а застосунок
 * сказав їй, що вона помилилася.
 *
 * Того ж дня так вийшло у трьох із п'яти: ×4,92 в Кулика, ×2,03 в Олександра,
 * ×1,9 в Ігоря. Не сходилося в жодного з них не через одометр.
 *
 * ЧОМУ ВИМКНЕНО, А НЕ ВИПРАВЛЕНО. Порівняння саме собою правильне й колись
 * ловило справжні описки (03.09 — 18 км при 94 за треком, 26.08 — 468 при
 * 105). Але воно має сенс лише тоді, коли трек повний. Поки ми лагодимо
 * запис, кожне таке вікно — це питання «чому не працює» від людини, яка
 * нічого зробити не може, і підозра в тому, чого вона не робила.
 *
 * ЩО ЛИШАЄТЬСЯ. Офіс бачить усе як бачив: адмінка, звіт про закриття в
 * Telegram (з чесним рядком «трек неповний»), пульт треку. Ховаємо рівно від
 * того, кому це число зараз не допомагає, — від торгового.
 *
 * ЯК ПОВЕРНУТИ. Один рядок: `true`. Умова повернення — не дата, а те, що
 * співвідношення в усіх сходиться без ручного втручання кілька днів поспіль
 * (дивитися `/admin/sales-analytics`, вкладка змін, колонку «одометр/трек»).
 * Разом із поверненням варто гейтити попередження ще й свіжістю треку — так,
 * як це вже робить `telegram-report.ts`: не звинувачувати людину, коли остання
 * точка старша за кілька десятків хвилин.
 *
 * Вимикач серверний навмисно: діє на всі планшети тієї ж миті, без оновлення
 * застосунку й без чекання, поки хтось натисне смугу оновлення.
 */
export const SHOW_TRACK_TO_STAFF = false;

/** `gpsDistanceKm` для відповіді застосунку — або число, або нічого. */
export function staffGps<T>(value: T): T | null {
  return SHOW_TRACK_TO_STAFF ? value : null;
}
