-- Чи підтверджує сама система живу підписку на локацію.
-- `tracking` каже лише «застосунок вважає, що пише»; 02.09 ці два стани
-- виявилися різними, і з сервера це було не видно.
ALTER TABLE "DeviceHeartbeat" ADD COLUMN "subscribed" BOOLEAN;
