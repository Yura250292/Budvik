-- Правки адміна на точках листа, що переживають обмін з 1С.
--
-- manual: точку додали на сайті (бонусна поїздка, «забрати з ремонту»).
--         У 1С її немає, тож прийом не має права її прибирати.
-- hidden: точку прибрали руками. Не видаляємо рядок, бо реалізація в 1С
--         лишається і наступний обмін привозив би точку назад щодня.
--
-- Обидва DEFAULT false: усе, що вже лежить у базі, приїхало з 1С і видиме.
ALTER TABLE "RouteSheetStop"
  ADD COLUMN "manual" BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN "hidden" BOOLEAN NOT NULL DEFAULT false;
