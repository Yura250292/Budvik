/**
 * Налаштування помічника торгового в одному місці.
 *
 * Причина, чому це окремий файл, а не константи по модулях: майже кожне
 * число тут — компроміс між «встигнути у вікно Vercel» і «дати моделі
 * досить даних». Коли вони розкидані, підкрутити один бік, не зламавши
 * інший, неможливо.
 */

import type { AssistantKind } from "@/lib/assistant/types";

/**
 * Модель — `deepseek-flash`, канонічною назвою. Це DeepSeek-V4.1-Flash.
 *
 * Перевірено пробою 11.09.2026 (`scripts/probe-deepseek-thinking.mts`):
 * `GET /models` цим ключем віддає рівно ДВІ назви — `deepseek-flash` і
 * `deepseek-v4-pro`. Попередні `deepseek-v4-flash` і `deepseek-chat` іще
 * відповідають, але перша — застарілий псевдонім тієї самої Flash, а друга
 * офіційно вимкнена 24.07.2026 і тримається вже без жодних обіцянок.
 *
 * Flash обрано навмисно: помічник обирає інструмент і переказує вже
 * пораховані числа. Через env — щоб перемкнути модель без деплою.
 *
 * ПРО PRO. З 14.09.2026 о 12:00 за Пекіном `deepseek-v4-pro` вимкнено, і
 * запит за цією назвою тихо обслуговує та сама V4.1-Flash. Тобто вписати
 * сюди pro більше не означає «взяти сильнішу модель» — це означає лишитися
 * на тій самій і думати, що взяв іншу. Коли потрібно глибше міркування,
 * його дає не інша модель, а THINKING_KINDS нижче.
 */
export const MODEL = process.env.ASSISTANT_MODEL || "deepseek-flash";

/**
 * Модель помічника керівника — Gemini, а DeepSeek лишається запасною.
 *
 * Рішення власника 16.09.2026: керівникові потрібна модель, що краще
 * зважує й пояснює, а торговий, водій і склад лишаються на швидкій і
 * дешевій DeepSeek. Ціна Gemini 3.x Flash — $0,75 / $3,75 за мільйон
 * (вхід / вихід, до 31.12.2026), тобто вп'ятеро дорожче за DeepSeek; при
 * ~30 тис. токенів на хід і сотні ходів на місяць це $15–20.
 *
 * Чому 3.6, а не 3.8. 16.09 вибір упав на 3.8: 3.7 тричі поспіль відповіла
 * 503 «high demand», а 3.8 працювала. Наступного дня 3.8 сама почала
 * відмовляти: з Vercel двічі 503 поспіль, тож на кожне питання керівника
 * відповідала запасна DeepSeek, а локально та сама модель думала 7–19 секунд
 * над словом «так». 3.6 відповідає за 1–3 секунди, а в порівнянні на 10
 * питаннях (scripts/assistant-eval.mts) дала таку саму якість. Ціна в них
 * однакова. Через env — щоб перемкнути без деплою, коли й ця почне падати.
 *
 * Запасна обовʼязкова, а не про всяк випадок: у день вибору 3.8 кілька
 * разів мовчала понад хвилину. Хід без запасної в такий день — порожній
 * екран; з нею — відповідь DeepSeek і позначка, хто відповів.
 *
 * КЛЮЧ. 18.09.2026 помічник переведено на ключ проєкту Budvik-Auth із
 * увімкненим білінгом (Tier 1): сплеск із 40 запитів проходить без жодної
 * відмови, у відповіді Google стоїть serviceTier «standard». Два попередні
 * ключі лишалися безкоштовними — 20 запитів на добу на модель і сім на
 * хвилину, — і після кількох питань керівникові відповідала DeepSeek.
 * Спільний GEMINI_API_KEY вітрини НЕ переводимо: жоден новий ключ уже не
 * бачить gemini-2.5-flash, на якій стоять одометр, перевірка цін, пошук фото
 * й збагачення описів (перевірено й на платному ключі 18.09).
 */
export const ADMIN_MODEL = process.env.ASSISTANT_ADMIN_MODEL || "gemini-3.6-flash";
export const FALLBACK_MODEL = MODEL;

export type LlmFlavor = "deepseek" | "gemini";

/**
 * Перемикач у кабінеті керівника обирає ПРОВАЙДЕРА, а не назву моделі.
 *
 * Назву моделі — а з нею гроші й ключ — вирішує сервер зі змінних
 * середовища, а браузер лише каже «Gemini» чи «DeepSeek». Так env лишається
 * єдиним місцем, де перемикається версія, і сторонній рядок у тілі запиту
 * нічого не відкриває.
 */
export const MODEL_FLAVORS: readonly LlmFlavor[] = ["gemini", "deepseek"];

export function modelForFlavor(flavor: LlmFlavor): string {
  if (flavor === "deepseek") return MODEL;
  return ADMIN_MODEL.startsWith("gemini") ? ADMIN_MODEL : "gemini-3.6-flash";
}

export type LlmProvider = {
  flavor: LlmFlavor;
  url: string;
  /** Змінна середовища з ключем — назва, не значення. */
  keyEnv: "DEEPSEEK_API_KEY" | "ASSISTANT_GEMINI_API_KEY";
  label: string;
};

const PROVIDERS: Record<LlmFlavor, LlmProvider> = {
  deepseek: {
    flavor: "deepseek",
    url: "https://api.deepseek.com/chat/completions",
    keyEnv: "DEEPSEEK_API_KEY",
    label: "DeepSeek",
  },
  /**
   * OpenAI-сумісний вхід Google, а не рідний generateContent.
   *
   * Формат повідомлень, інструментів і стріму той самий, що в DeepSeek,
   * тож цикл ходу, інструменти й склеювання викликів не роздвоюються.
   * Відмінностей три, і всі вони в llm.ts: підпис думки при кожному
   * виклику, фрагменти без index і reasoning_effort замість thinking.
   */
  gemini: {
    flavor: "gemini",
    url: "https://generativelanguage.googleapis.com/v1beta/openai/chat/completions",
    keyEnv: "ASSISTANT_GEMINI_API_KEY",
    label: "Gemini",
  },
};

/**
 * Ключі провайдерів для помічника — з середовища.
 *
 * Gemini помічника має СВІЙ ключ (ASSISTANT_GEMINI_API_KEY), окремий від
 * спільного GEMINI_API_KEY вітрини. Причина перевірена 17.09.2026: у проєкті
 * нового ключа Google уже не віддає gemini-2.5-flash «новим користувачам», а
 * на ній жорстко стоять розпізнавання одометра, перевірка цін, пошук фото й
 * збагачення описів. Заміна спільного ключа поклала б їх; окремий ключ ще й
 * розводить рахунки: помічник платить зі свого проєкту.
 * Немає окремого — береться спільний, як було до 17.09.
 */
export function assistantKeys(): Partial<Record<LlmFlavor, string[]>> {
  /**
   * Порядок ключів Gemini — це порядок витрат: спершу безкоштовний.
   *
   * Рішення власника 18.09.2026: «щоб зекономити». Спільний GEMINI_API_KEY
   * сидить на безкоштовному тарифі (20 запитів на добу на модель), і поки він
   * відповідає, платити нема за що. Щойно він упирається в квоту, той самий
   * хід доходить платним ASSISTANT_GEMINI_API_KEY, а вичерпаний ключ
   * пропускається наступну годину (model-health.ts) — інакше кожен хід
   * починався б із гарантованої відмови.
   *
   * Якщо ключ один, список із одного елемента: поведінка як була.
   */
  const gemini = [process.env.GEMINI_API_KEY, process.env.ASSISTANT_GEMINI_API_KEY]
    .map((k) => k?.trim())
    .filter((k): k is string => Boolean(k));
  return {
    deepseek: process.env.DEEPSEEK_API_KEY ? [process.env.DEEPSEEK_API_KEY] : undefined,
    gemini: gemini.length > 0 ? [...new Set(gemini)] : undefined,
  };
}

/** Провайдер за назвою моделі — префікс однозначний. */
export function providerFor(model: string): LlmProvider {
  return model.startsWith("gemini") ? PROVIDERS.gemini : PROVIDERS.deepseek;
}

/** Нижче — менше фантазій у формулюваннях; вище — жвавіший текст. Лише DeepSeek. */
export const TEMPERATURE = 0.3;

/** Стеля відповіді: план дня на 10 точок з поясненнями ще вміщається. */
export const MAX_TOKENS_FINAL = 1600;

/**
 * Кому даємо міркування — і тільки тому.
 *
 * Заміряно пробою `scripts/probe-deepseek-thinking.mts` на питанні «у трьох
 * торгових такі обороти й така дебіторка, кому не давати відстрочку»:
 * без міркувань модель обрала НЕ ТОГО і пояснила це плутано, з
 * міркуваннями — обрала правильного й назвала причину (дебіторка 84%
 * обороту). Це рівно той клас питань, з якими приходять до помічника
 * керівника: зважити й проранжувати, а не знайти.
 *
 * Чому не всім. Торговий, водій і складовщик питають «де», «скільки» й
 * «кому везти». Там міркування додають секунди й токени, не додаючи
 * правильності, — а саме для цього все й будувалося швидким.
 *
 * Кого це насправді охоплює: вид ADMIN дає kindForThread лише офісу
 * (ADMIN і MANAGER) і лише в розмові про ВСЮ фірму. Щойно керівник обирає
 * конкретного торгового, вид інший, і хід знову швидкий — тобто думання
 * вмикається саме там, де питання загальне й відповідь треба зважити.
 *
 * Заміряно на бойовому прогоні 11.09.2026 («середній чек у серпні проти
 * вересня, і чи це через менші замовлення, чи через менше клієнтів»):
 * без міркувань модель порівняла повний серпень з 11 днями вересня й
 * відповіла «впало все одразу» — неправильно; з міркуваннями перевела
 * обидва періоди в денні темпи й знайшла справжню причину. Ціна: 45 с
 * проти 9 с і 80 тис. токенів проти 28 тис.
 *
 * Вимкнути цілком, не чіпаючи код: ASSISTANT_THINKING=off.
 */
export const THINKING_KINDS: readonly AssistantKind[] = ["ADMIN"];

/** Запобіжник на випадок, якщо міркування почнуть шкодити в проді. */
export const THINKING_ENABLED = process.env.ASSISTANT_THINKING !== "off";

/**
 * Стеля відповіді, коли модель міркує.
 *
 * Токени міркувань входять у `max_tokens` РАЗОМ із текстом відповіді: у
 * пробі з 292 вихідних токенів 238 були міркуванням. Лишити тут 1600
 * означало б, що на складному питанні міркування зʼїсть усю стелю, і
 * замість відповіді користувач отримає позначку «обірвано за лімітом».
 *
 * 16.09.2026 піднято з 6000 до 10 000: керівник отримав нову структуру
 * відповіді (плитки, висновок, що робити, діаграма), і на питанні «що
 * замовити на наступний місяць» DeepSeek витратив ~6 тис. на роздуми за хід
 * і обірвав відповідь на третьому пункті. Час обмежує не стеля токенів, а
 * бюджет виклику в циклі ходу.
 */
export const MAX_TOKENS_THINKING = 10_000;

/**
 * Менше цього часу до дедлайну ходу — міркування вимикаємо.
 *
 * Думання відкладає перший видимий символ на секунди. Якщо хід уже на
 * межі, чесніше швидко відповісти з того, що зібрано, ніж обірвати роздум
 * на півслові й не сказати нічого.
 */
export const THINKING_MIN_MS = 35_000;

/**
 * Скільки разів модель може сходити по інструменти за одне питання.
 *
 * Чотири: знайти клієнта → профіль → пропозиція → відповідь. П'ятий раунд
 * на практиці означає, що модель ходить по колу, а користувач чекає.
 */
export const MAX_ROUNDS = 4;

/** Стеля викликів інструментів на хід — щоб один запит не з'їв усе вікно. */
export const MAX_TOOL_CALLS_PER_TURN = 10;

/**
 * Скільки інструментів запускаємо одночасно.
 *
 * Троє: пул Prisma спільний з рештою сайту, а найважчі запити тут
 * (дебіторка, дисципліна) читають усю таблицю документів.
 */
export const TOOL_CONCURRENCY = 3;

/** Один похід до моделі. */
export const CALL_TIMEOUT_MS = 40_000;

/**
 * Скільки лишити ходу на збереження відповіді після останнього виклику.
 *
 * Бюджет виклику рахується від дедлайну ходу, а не береться сталим: інакше
 * повтор і запасна модель складалися б із трьох незалежних 40-секундних
 * таймаутів — більше, ніж живе роут.
 */
export const SAVE_RESERVE_MS = 8_000;

/** Менше цього на виклик — не йдемо до моделі взагалі, а чесно кажемо «не встиг». */
export const MIN_CALL_MS = 6_000;

/**
 * Пауза перед повтором на ту саму модель.
 *
 * Повтор — лише після ШВИДКОЇ відмови (503/429 за секунду-дві). Після
 * таймауту повтор марний і з'їдає час запасної моделі.
 */
export const RETRY_DELAY_MS = 800;

/**
 * Увесь хід. Менше за maxDuration роута (120 с), бо після дедлайну ще
 * треба встигнути дописати відповідь і зберегти її.
 */
export const TURN_DEADLINE_MS = 100_000;

/**
 * Коли лишається менше — забороняємо інструменти й вимагаємо текст.
 *
 * Інакше модель встигала б замовити ще один інструмент рівно перед
 * обривом, і користувач бачив би «Дивлюся борги…» без жодної відповіді.
 */
export const FINAL_ONLY_BELOW_MS = 25_000;

/**
 * Стеля на результат одного інструмента, символів.
 *
 * ~3,5 тис. токенів. Більше не потрібно: усі інструменти віддають готові
 * підсумки, а не сирі рядки, і те, що не влізло, однаково не вплинуло б
 * на пораду.
 */
export const TOOL_RESULT_MAX_CHARS = 12_000;

/**
 * Скільки попередніх реплік бачить модель (службові TOOL не рахуються).
 *
 * 24 тисячі символів — це вісім тисяч токенів історії В КОЖНОМУ раунді
 * ходу, тобто до тридцяти тисяч за одну відповідь. Заміряно на бойовій
 * розмові: більшу частину з'їдали не питання, а власні переліки помічника
 * (список залишків на 2,5 тис. символів). Для «а що з другим клієнтом»
 * досить початку попередньої репліки — імена й посилання стоять там.
 */
export const HISTORY_MAX_MESSAGES = 12;
export const HISTORY_MAX_CHARS = 8_000;

/** Довжина питання. Довші — це вже не питання, а вставлений документ. */
export const USER_TEXT_MAX = 1_000;

/**
 * Ходів на людину на добу.
 *
 * Не про економію, а про запобіжник від циклу в інтерфейсі: торговий за
 * день ставить 10–20 питань, 150 недосяжні при звичайній роботі.
 */
export const DAILY_TURN_CAP = 150;

/**
 * Мінімальна маржа, нижче за яку «солодка ціна» стає збитковою.
 *
 * Гачок дають дешево навмисно — на ньому не заробляють, ним заходять. Але
 * продавати нижче собівартості не можна навіть заради входу, тож підлога
 * = собівартість + 5%. Остаточну знижку однаково затверджує керівник:
 * собівартість тут з останньої реалізації, це оцінка, а не факт.
 */
export const HOOK_MIN_MARGIN = 0.05;

/**
 * Скільки різних клієнтів мають брати товар, щоб він вважався гачком.
 *
 * Гачок — це те, що бере вся база: з ним заходять до незнайомого магазину
 * і майже напевно вгадують. Десять клієнтів за півроку — межа, з якої
 * товар перестає бути чиєюсь окремою потребою.
 */
export const HOOK_MIN_CLIENTS = 10;

/** Вікно, за яким рахуються звички закупівель. */
export const HOOK_WINDOW_DAYS = 180;

/** Мінімум клієнтів портфеля, щоб радити гачок, якого цей клієнт не бере. */
export const HOOK_PEER_MIN = 3;

/**
 * Причіп: пороги ті самі, що в аналізі кошика (basket.ts).
 *
 * MIN_TOGETHER=5 — нижче випадковість; MIN_LIFT=1.5 — нижче це просто два
 * бестселери, які трапляються разом, бо трапляються скрізь.
 */
export const ATTACH_MIN_TOGETHER = 5;
export const ATTACH_MIN_LIFT = 1.5;

/**
 * З якого «перебору циклу» гачок вважається терміновим.
 *
 * Той самий поріг, що в рекомендаціях картки клієнта (OVERDUE_MIN у
 * clientOrder.ts): запас у 20% на випадок, коли клієнт і сам збирався
 * замовити.
 */
export const OVERDUE_HOOK_MIN = 1.2;

/** Не радимо як причіп те, що клієнт брав нещодавно — він і сам візьме. */
export const ATTACH_NOT_BOUGHT_DAYS = 90;

/** Скільки причепів показуємо на один гачок. */
export const ATTACH_PER_HOOK = 4;

/** Скільки днів без продажу робить залишок «мертвим». */
export const DEAD_STOCK_DAYS = 90;

/**
 * Розхідники: те, з чим заходять у магазин.
 *
 * Розділи цілком: оснастка (свердла, круги, диски), кріплення, скотч і
 * плівка. Плюс окремі види з інших розділів — піна, зварювальні
 * електроди й дріт, ліска до мотокоси, ланцюг до пилки.
 *
 * Значення звірені з базою 04.09.2026 (classify.ts дає їх у Product.typeKey);
 * вигаданих ключів тут немає — інакше список гачків мовчки лишився б порожнім.
 */
export const CONSUMABLE_SECTIONS = ["osnastka", "krip", "skotch"] as const;
export const CONSUMABLE_TYPE_KEYS = [
  "піна",
  "герметик",
  "клей",
  "електроди",
  "зварювальна-оснастка",
  "ліска",
  "ланцюг-шина",
] as const;

/**
 * Запасний розпізнавач для товарів без typeKey — а таких 4995 активних.
 *
 * Саме тут ловляться «круг відрізний», «дріт зварювальний» і «піна
 * монтажна» з тих карток, які класифікатор не розібрав.
 */
export const CONSUMABLE_NAME_RE =
  /(круг|диск)\s+(відрізн|зачисн|шліфув)|відрізн\w*\s+(круг|диск)|дріт\s*.{0,20}(зварюв|п\/авт|порошков|омідн)|(монтажн\w*\s+піна|піна\s+монтажн)|ліска|ланцюг\s+пильн|електрод/i;
