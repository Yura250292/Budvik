import { redirect } from "next/navigation";

/**
 * Хаб «Звіти» розібраний: він був лише сіткою посилань на сторінки, які
 * тепер напряму в меню «Аналітика та звіти» (звіти складу, бухгалтерські
 * звіти), а «Звіти торгових» — вкладка «Поїздки» аналітики торгових.
 *
 * Редірект, а не видалення: посилання на /admin/reports розійшлися по
 * закладках та інструкціях. Ведемо на звіти складу — найчастішу ціль хабу.
 */
export default function ReportsHubRedirect() {
  redirect("/admin/warehouse-reports");
}
